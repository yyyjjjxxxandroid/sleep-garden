# 眠境花园 · 3D 版使用说明

这是面向手机竖屏的黑客松演示：在可环绕、缩放的 3D 花园里听自然声，看小猫散步，并在陪伴后留下一株植物。

## 打开方式

直接用支持 WebGL 的现代浏览器打开同目录的 `index.html`。三维引擎、模型及页面代码已打包进这个单文件，运行时不需要下载模型或连接 CDN。声音由浏览器本地合成，点击「开始陪我」后启用。

手机上需要在浏览器中打开 HTML；系统文件预览器可能不执行脚本。若通过局域网在手机浏览器演示，可在本目录运行 `python3 -m http.server 8765`，再访问电脑的局域网 IP 与端口。

`二维版备份.html` 保留改版前的页面，供回看和对比。

## 关键交互

| 操作 | 交互 |
| --- | --- |
| 探索花园 | 单指拖动环绕，双指缩放及移动；右侧按钮放大、缩小或返回全景。 |
| 看小猫 | 小猫按预设路线散步并间歇停留；点击小猫或「靠近小猫」切换近景，返回全景后继续探索。 |
| 开始陪伴 | 点击「开始陪我」使用推荐混音。演示默认 90 秒，可暂停、继续或提前结束。 |
| 调整声音 | 「调整一下」中独立调节阳光暖调氛围音、风声、雨声、虫鸣、猫咪呼噜及流水声，也可单独开关。 |
| 调整环境 | 「天气与风」集中提供晴天、彩虹、雨天、极光、流星、风速和阳光强度。极光、流星进入夜晚；调整阳光会切回白天。 |
| 夜间与动效 | 环境面板的更多设置中可切换昼夜、夜间萤火虫、风速同步风声音量及减少动态效果。 |
| 陪伴结果 | 结束后留下植物并填写简短放松反馈；「我的花园」查看记录、再次播放相似声音。 |

阳光的**画面强度**与「阳光」的**暖调氛围音量**是两项独立控制。风速作用于植物局部变形与雨线倾斜；开启声音联动时，也同步风声音量。

当前动物行为是路线移动配合模型自带骨骼动画；近景时暂停路线移动。声音是 Demo 的合成占位音效，并非自然环境实录。个人记录保存在当前浏览器的本地存储中。

## 文件与重建

| 文件 | 用途 |
| --- | --- |
| `index.html` | 可直接打开的 3D 单文件成品。 |
| `source.html` | 页面、声音与陪伴流程源码，构建时替换为三维世界。 |
| `world3d.js` / `world3d.css` | 三维场景、动物、天气及 3D 页面样式。 |
| `world.css` | 共用的世界优先布局与控制面板样式。 |
| `assets3d/` | 下载的 GLB 模型与 Kenney 授权文件。 |
| `vendor/three/` | 本地 Three.js 及加载器、镜头控制等依赖。 |
| `design-skills/` | 本项目参考的设计技能副本。 |
| `build3d.py` | 打包 JavaScript、嵌入 GLB 与样式，输出 `index.html`。 |
| `build.py` / `package.json` | 默认 3D 构建入口及 esbuild 开发依赖。 |
| `world3d.bundle.js` | 构建生成的三维脚本包。 |

在本目录运行：

```sh
npm install
npm run build
```

也可以运行 `python3 build.py` 或 `python3 build3d.py`。构建器依次查找 `GARDEN_ESBUILD` 指定路径、系统 esbuild、本地 `node_modules/.bin/esbuild`，最后尝试本机临时路径 `/private/tmp/garden-esbuild/bin/esbuild`。`npm install` 会准备本地依赖，安装时需要网络，成品 HTML 运行时不需要。

若没有可用的 esbuild 且 `world3d.bundle.js` 不早于 `world3d.js`，构建器会沿用已有脚本包；修改三维脚本后脚本包过期，则提示先安装依赖。`build2d.py` 是旧版打包器，运行会把 `index.html` 覆盖为二维版；仅回看旧版时直接打开 `二维版备份.html` 即可。

素材作者、授权及现成素材与自制效果的区分，见同目录《素材与技能来源.md》。本轮已在浏览器的 390 × 844 手机尺寸视口检查；这不等于覆盖各型号手机的真机性能测试。
