# 眠境花园团队分工

## 当前项目状态

我们已经有一个可以直接打开的 Web 3D Demo，不是从零开发新 App：

- `index.html`：当前可演示的单文件成品。
- `source.html`：页面结构、声音控制和陪伴流程源码。
- `world3d.js`、`world-base.js`、`story-world.js`：三维世界和镜头基础。
- `story-cat.js`、`story-detail.js`、`story-atmosphere.js`：小猫、场景细节和氛围效果。
- `world*.css`、`story-world.css`：页面布局和控制面板样式。
- `assets3d/`、`assets3d-v2/`、`audio/`：模型、贴图和声音素材。
- `build3d.py`：把源码和素材重新打包成 `index.html`。

下面是基于现有 Demo 的后续迭代分工。五位同学都是开发，不设 Android、后端、算法或专职测试岗位；“负责人”只表示这一块由谁牵头，其他人可以直接协助和提交代码。

## 五位开发负责人

| 负责人 | 主要改动范围 | 这轮交付 |
| --- | --- | --- |
| 开发 1：主流程与交互 | `source.html`、交互状态、开始/暂停/结束/反馈流程 | 把“进入花园 → 开始陪伴 → 播放中 → 结束反馈”整理成稳定、少步骤的演示链路 |
| 开发 2：3D 场景与镜头 | `world3d.js`、`world-base.js`、相机、光照、雾效、性能 | 优化首屏构图、镜头操作和移动端帧率，不改变现有核心流程 |
| 开发 3：小猫与氛围表现 | `story-cat.js`、`story-detail.js`、`story-atmosphere.js`、`assets3d/` | 优化小猫待机/靠近/陪伴表现，补充萤火虫、风、雨、昼夜等氛围细节 |
| 开发 4：声音与页面视觉 | `audio/`、`world*.css`、`story-world.css`、声音/天气面板 | 调整声音组合、按钮和面板层级，保证手机竖屏下文字、控件和状态清晰 |
| 开发 5：构建整合与演示 | `build3d.py`、`package.json`、`index.html`、说明文档 | 保证源码能一键构建出可运行成品，处理资源路径、浏览器兼容、演示脚本和最终问题 |

你（产品兼开发）负责：确定本轮要解决的用户问题和优先级，维护主流程验收表，组织合并和每周演示；需要时直接参与任一模块开发。

## 工作方式

1. 先把当前 `main` 版本作为基线，不直接大范围重写已有 Demo。
2. 每位负责人从 `main` 创建分支，只处理自己这一轮的改动范围；跨模块改动先在群里说明。
3. 每个功能先在现有网页里跑通，再考虑整理代码或替换素材。
4. 不做真实接口、账号系统和 Android 容器；声音方案、记录和 AI 行为先用本地 JavaScript/JSON/LocalStorage 模拟。
5. 每次合并前提供：运行方式、改动截图或录屏、是否影响主流程。

## 分支命名

```text
main                 当前稳定演示版
feat/flow-*          主流程与交互
feat/scene-*         3D 场景与镜头
feat/cat-*           小猫、素材与氛围
feat/audio-ui-*      声音、天气面板与页面视觉
feat/build-demo-*    构建、兼容性与演示整合
```

## 本轮统一验收

- 浏览器打开 `index.html` 能进入花园，首屏没有空白或资源报错。
- 用户能完成开始陪伴、播放控制、调整声音、结束反馈这条链路。
- 手机竖屏下按钮和文字不重叠，拖动/缩放/面板开关可用。
- 3D 氛围升级不能让主流程变得更难操作；素材加载失败时仍有可用的降级表现。
- 修改源码后运行 `python3 build3d.py`，生成的 `index.html` 与源码行为一致。
