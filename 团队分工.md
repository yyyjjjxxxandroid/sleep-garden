# 眠境花园团队分工

六人配置：你负责产品兼开发，另外五位同学都是全栈开发。下面的划分是“模块负责人”，不是固定岗位；每位负责人都可以同时修改页面、Android 容器、Mock 数据和联调代码，谁先完成谁先合并。

## 模块负责人

| 模块 | 负责人 | 负责内容 | 首个可交付结果 |
| --- | --- | --- | --- |
| 产品流程与主链路 | 你（产品兼开发） | 定义用户旅程、页面状态、交互文案、MVP 范围；维护验收清单并推动联调 | 用户说出状态 → 生成方案 → 开始陪伴 → 结束反馈的一条可演示流程 |
| 声音方案与 AI 体验 | 开发 1 | 设计情绪/目标标签、自然语言输入解析、声音配方和宠物回应；先用本地规则或 Mock JSON，不接真实后端 | 输入 3-5 种典型表达，稳定返回不同的声音方案 |
| 3D 花园与宠物表现 | 开发 2 | Three.js 场景、宠物动画、灯光、天气氛围、播放状态视觉反馈和性能 | 花园首屏、宠物待机/陪伴/结束三种状态，可替换资源 |
| 播放与 Android Demo | 开发 3 | Android 容器、音频播放/暂停/定时/渐弱、后台播放、耳机按键和基础 TalkBack | 真机可安装运行，能完整播放一段声音并正确切换状态 |
| 交互整合与演示质量 | 开发 4 | 页面与 Android 联调、数据持久化（LocalStorage/文件即可）、响应式适配、异常兜底、演示脚本和录屏 | 一键启动的稳定 Demo，覆盖主流程、空状态和失败提示 |
| 体验验证与优化 | 开发 5 | 与前面模块一起开发；负责真机检查、无障碍走查、性能检查、用户试用反馈和问题修复 | 验收表、问题清单和最终演示版本；不单独建设测试系统 |

> 五位开发都可以跨模块提交代码。表格只是首轮负责人，遇到阻塞直接结对处理，不等待“后端、算法或测试岗位”排期。

## Demo 数据策略

首版不建设后端服务：

- 声音方案、宠物回应和情绪标签放在本地 JSON/JavaScript 配置中。
- 播放记录和用户偏好用 LocalStorage 或 Android 本地文件保存。
- 如果以后需要真实接口，先把本地数据层封装成同样的函数，再替换实现，不改变页面交互。
- AI 先做“规则 + Mock 返回”，重点验证体验，不把时间花在模型部署和账号系统上。

## MVP 验收链路

1. 用户输入或说出“今晚的状态”。
2. 系统识别少量状态标签，例如焦虑、疲惫、独处、入睡困难。
3. 返回一套声音配方：音乐/自然声/宠物陪伴/时长/渐弱。
4. 用户可以开始、暂停、换一套、关闭人声、结束。
5. 结束后只问一个放松度问题，并在本地保存一次陪伴记录。
6. 3D 花园和无障碍流程服务于这条主链路；手表联动、复杂游戏化和真实云端服务放到后续版本。

## 分支约定

```text
main                 可演示稳定版本
feat/flow-*          主流程和页面状态
feat/sound-*         声音方案、AI Mock 和宠物文案
feat/scene-*         3D 花园、宠物和氛围
feat/android-*       Android 播放、后台和无障碍
feat/integration-*   联调、适配、验收和演示修复
```

提交信息使用：

```text
feat: 增加声音方案推荐
fix: 修复播放状态切换
art: 替换花园场景资源
perf: 优化移动端场景性能
test: 增加主流程验收记录
docs: 更新演示说明
```

## 协作节奏

- 第一次组会：确定每条任务线负责人、演示设备和本周唯一目标。
- 每天同步：只说“完成、阻塞、下一步”，阻塞超过半天就找另一位同学结对。
- 合并前：负责人提供运行方式、改动截图或录屏，以及是否影响主流程的说明。
- 每周演示：从干净环境走一遍主链路，记录问题并只修复影响演示的高优先级问题。
