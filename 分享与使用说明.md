# 分享与使用

## 给体验者

只发 `index.html` 即可。三维模型、贴图、脚本和样式已经内嵌，声音由浏览器本地合成，不需要附带整个项目文件夹。

下载 HTML 后用支持 WebGL 的浏览器打开，点击「开始陪我」启用声音。聊天软件或手机文件管理器的预览不一定执行网页脚本。`127.0.0.1` 是本机预览地址，不能作为公开分享链接；需要点链接即打开时，可将 HTML 部署到静态网站。

## 当前交互

- 陪伴默认不限时，可随时手动结束。
- 控制条点「不限时」或声音页点「设置定时」，选择 15、30、60 分钟或自定义 1–720 分钟。
- 播放中设置定时，从设置当下计算；暂停同时暂停倒计时，可随时取消。
- 声音和天气均为半屏面板，内容可以滚动，拖动顶端短横线向下关闭。
- 小猫已缩小为原尺寸的 65%。新形象见 `小猫候选.html`，候选预览图需联网，尚未购入或替换。

## 本次验证

已检查默认不限时、自定义定时、暂停倒计时、取消定时、声音半屏布局及下滑关闭。渲染控制台无新错误。溪流使用与池塘共享的世界坐标倒影，以控制移动端开销。

## 源码交接

开发人员继续修改时，交接项目目录；普通体验者仅需 `index.html`。原二维备份保留在 `二维版备份.html`。
