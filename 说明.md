# 眠境花园 · 手机 HTML Demo

打开 `index.html` 即可使用。页面为手机竖屏 H5；在电脑浏览器打开时以手机宽度居中展示。插画、动效、声音逻辑已经全部内嵌，约 10 MB，HTML 可单独复制给队友。

## 可探索的世界

- 单指拖动或鼠标拖动移动镜头，有轻微惯性。
- 双指捏合或鼠标滚轮缩放，也可点侧边 + / −。缩放范围 1–2.8 倍。
- 侧边取景框按钮回到初始全景；键盘方向键也可移动。
- 点击小猫平滑推近；返回花园保留同一条声音和生长时间线。
- 池塘、树梢和草地上的小声源按钮可直接进入对应的声音调节。
- 场景采用 imagegen 生成的手绘大地图与透明小猫图层。WebGL 网格局部形变让柳枝与花草响应风力，不是整张图一起晃动；雨线、水波、萤火虫和植物为独立图层。
- 夜间对世界统一调色，萤火虫分散漂移、错峰明灭。按钮保留触控面积，视觉上收敛在边缘。

这是可移动镜头的 **2.5D 插画世界**，保留用户参考风格；不是完整三维建模场景。

## 演示路径

1. 点击大按钮「开始陪我」，直接播放推荐声景。
2. 点击「调整一下」，独立调节阳光暖调氛围、风、雨、虫鸣、猫咪呼噜、流水。点每项左侧图标可静音/恢复。
3. 同一抽屉切到「风与光」：调风吹动效、阳光亮度、风声联动、萤火虫开关。把风力拉到 0，柳枝和花草停止摆动；拉高可观察明显差异。
4. 右上角月亮按钮切到夜晚，萤火虫缓慢漂移、交错明灭。
5. 点小猫或「靠近小猫」推近镜头；轻摸小猫有回应。返回全景，声音与进度不中断。
6. 暂停/继续共用时间线。90 秒演示陪伴结束后生成植物；也可以提前「结束陪伴」，保留幼株。
7. 结果卡可选填写反馈，回到花园。点击植物或右上角叶片查看记录，重新播放相似声音。

## 声音模块

| 控件 | 音效 | 画面联动 |
|---|---|---|
| 阳光 | 低音量暖调氛围合成音 | 「风与光」另有阳光亮度；阳光本身不被当成真实自然声音 |
| 风声 | 滤波噪声、缓慢起伏 | 默认同步风吹强度，可解除联动 |
| 雨声 | 高频与滤波噪声混合 | 雨丝浓淡随音量变化 |
| 虫鸣 | 稀疏脉冲与高频鸣音 | 声音独立控制 |
| 猫咪呼噜 | 低频谐波、脉冲与呼吸包络 | 聚焦略靠近；猫身缓慢起伏 |
| 流水 | 滤波噪声与轻水滴合成 | 池塘水纹 |

这版是**本地程序合成音效**，用于体验与混音演示，不是 AI 模型生成或真实录音。音频只在用户点击开始后播放。没有上传用户数据、联网请求、账号或 API Key。

`audio/` 提供 6 段 16 秒立体声 WAV，可单独试听、交给音频同学替换；`generate_audio.py` 是可复现的生成脚本。HTML 不依赖这些外部文件，自己生成同类声层，因此双击打开仍能播放。

## 产品方案对应

依据 `md/mini项目疗愈产品方案_2026-09-09.md` 的首页、全景、聚焦、调节、植物结果、我的花园六种关键状态实现；增加用户要求的虫鸣、阳光、可调风吹及夜间萤火虫。

默认以细雨为主，按当前雨/风/水主声层生成雨蕨/风铃草/水叶草；音量偏好与植物记录保存在当前浏览器。界面标明 90 秒演示模式。提前结束保留幼株，不计作完整成长。

画面参考用户上传的花园、垂柳、池塘截图与已确认的低饱和插画方向。小红书链接可读取笔记，但视频被登录弹窗遮挡；没有假定完整看过视频。

使用 imagegen 技能和内置图片生成完成场景底图与透明小猫，提示词见 `生成提示词.md`。另使用 skill-installer 查找音频技能、fal-media / model-routing 检索并尝试音效生成；fal 返回余额不足，未生成音频、未获得任务 ID，因此本版保留程序合成声轨。选定的后续音效端点是 `fal-ai/elevenlabs/sound-effects/v2`，可循环、计划每条 12 秒；账户充值后才可重新提交。

## 文件

- `index.html`：打包后的手机 H5 单文件，直接使用这个文件。
- `source.html`、`world.css`、`world.js`：可编辑的源文件。
- `assets/`：生成的世界底图、小猫透明图。
- `build.py`：将代码与图片内嵌为 `index.html`，执行 `python3 build.py` 即可重新打包。
- `audio/`：6 段可试听 WAV。
- `generate_audio.py`：合成音效生成代码，依赖 NumPy。

若手机聊天软件的文件预览不播放音频，请使用手机浏览器打开。iOS 锁屏与后台播放由浏览器策略决定；本 Demo 不保证系统级后台续播。浏览器本地记录不跨设备同步。
