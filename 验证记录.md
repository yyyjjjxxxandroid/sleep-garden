# 验证记录

本地浏览器验证，2026-09-17。

- 单文件打包：HTML 内包含插画 data URI、样式、脚本；外部资源依赖检查为空。
- 两段 JavaScript 语法检查通过。
- 手机窄屏预览：首页、全景、聚焦、声音抽屉、风光抽屉、植物结果与历史记录正常显示；长抽屉可滚动。
- 一键开始后 Web Audio 初始化成功，界面记录 running；暂停/继续按钮状态正确。
- 风声音量调到 100，风力联动显示 100%；风吹动效调到 0，风声音量同步为 0。
- 昼夜切换、萤火虫图层、小猫推近镜头可用。
- 放大后拖拽，镜头中心 X 从约 535 移到约 673；界面缩放显示 1.2×。
- 提前结束显示幼株结果；可反馈、进入历史记录并再次播放相似声景。
- 完整运行 90 秒后自动结束，显示「花园里，多了一株雨蕨」和陪伴 01:30。
- 刷新后，声音偏好与已生成的植物保留。
- 检查期间浏览器没有 error / warn 日志。
- 6 段 WAV 均为 16 秒、32 kHz、双声道，已检查峰值未削波。

未实测真实手机硬件、iOS 锁屏后台续播、真实双指硬件手势。双指缩放已实现；本轮通过鼠标拖拽、缩放按钮与键盘验证共享镜头逻辑。浏览器自动化不允许访问 file URL，因此通过本地 HTTP 测试同一单文件；文件内没有远程依赖。

AI 音效生成遇到 fal 余额不足，交付音效为本地程序合成。没有声称生成音频已被人工听感验证。
