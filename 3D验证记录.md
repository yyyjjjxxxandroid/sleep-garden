# 3D Demo 验证记录

验证日期：2026-09-18。环境：桌面内置 Chromium 浏览器，390 × 844 手机视口；通过 localhost 加载与离线成品相同的 index.html。未进行各型号手机真机测试。

- 成品已嵌入 Three.js、13 个 GLB 文件与页面逻辑，约 3.75 MB；浏览器模型加载完成。
- 猫模型成功加载 Animation 骨骼动画；时间推进时世界坐标变化，近景可看见猫模型，退出后恢复全景。
- 全景、靠近小猫、天气切换、声音面板、暂停及提前结束流程已通过 UI 操作验证。
- 6 路声音滑杆及开关保留；点击开始后 Web Audio 状态为 running。未做主观音质测评。
- 阳光滑杆 0 / 100 时，实际场景灯光强度分别为 0.25 / 4.05（晴天）；并非只改变按钮数字。
- 彩虹在远景中显示完整上弧；极光使用夜间天空光幕；雨天和流星切换可正常进入对应状态。流星为间歇出现。
- 风速 100 可设置并同步风声音量；风动只改变植物的局部顶点。树干根部固定。
- 植物素材实例化后，在检查的默认镜头中约 48 次绘制调用（具体数量随镜头和天气变化）；不是帧率保证。
- 提前结束会创建幼株反馈，回到花园与历史记录入口正常。
- 初次检查发现负值插入 GLSL 时形成双减号；已加括号修正并重新编译。后续模型与风动材质正常显示。

旧二维成品已保留于「二维版备份.html」。当前默认构建入口 build.py 输出 3D 版本。
